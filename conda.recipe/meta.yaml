package:
  name: ppap4lmp
  version: 0.1.2a

build:
  number: 1

source:
  git_url: ../

requirements:
  build:
    - {{ compiler('cxx') }}
  host:
    - python
    - setuptools
    - pip
    - nlohmann_json
    - pybind11
    - eigen
    - cmake
  run:
    - python
    - numpy

test:
  source_files:
    - tests

about:
  summary: PostProcess and Analysis Program for LAMMPS
  license_file: LICENSE
